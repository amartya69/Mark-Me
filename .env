# PostgreSQL connection string (replace with actual value in .env)
DATABASE_URL=postgresql://username:password@hostname:5432/attendance_db

# Secret key for JWT or other security features
SECRET_KEY=your-secret-key-here

# Optional: if you want to run locally
# (Render uses PORT=10000 automatically, but locally you can use 8000)
PORT=8000
